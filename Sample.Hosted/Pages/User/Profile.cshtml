@page
@using Microsoft.AspNetCore.Mvc.TagHelpers
@using Sample.Hosted
@using Sample.Hosted.Utils
@model Sample.Hosted.Pages.User.ProfileModel
@{
    ViewData["Title"] = "User Details";
}

<h2>User Profile</h2>

@if (Model.IdentityUser != null)
{
    <div class="card">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">User ID</dt>
                <dd class="col-sm-9">@Model.IdentityUser.Id</dd>

                <dt class="col-sm-3">User Name</dt>
                <dd class="col-sm-9">@Model.IdentityUser.UserName</dd>

                <dt class="col-sm-3">Email</dt>
                <dd class="col-sm-9">@Model.IdentityUser.Email</dd>

                <dt class="col-sm-3">Phone Number</dt>
                <dd class="col-sm-9">@Model.IdentityUser.PhoneNumber</dd>

                <dt class="col-sm-3">Slim UI</dt>
                <dd class="col-sm-9">@ClaimFor.Profile.SlimUi</dd>
                <!-- Add more user details here if necessary -->
            </dl>
        </div>
    </div>

    <a asp-page="@PageFor.User.ProfileEdit" class="btn btn-primary mt-3">Edit Profile</a>
    <a asp-page="@PageFor.User.ProfilePicture" class="btn btn-secondary mt-3">Change Profile Picture</a>
}
else
{
    <div class="alert alert-warning">
        No user information available.
    </div>
}